Family First ND Panel
Features
Role-based management (ND, SS, DB, Retailer, Admin)
Paginated lists and filters via API
Key generation, transfers, inventory and validity timeline
Token-based auth with auto-refresh logic in api.ts
Admin profile and password management
CSV export for key transfer logs
Prerequisites
Node.js (LTS recommended)
pnpm (or npm/yarn)
A running backend (see API base URL below)
Environment
Configure environment variables in .env. Important variable:
